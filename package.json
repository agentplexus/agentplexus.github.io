{
  "name": "agentplexus.github.io",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @agentplexus/web",
    "build": "npm run build -w @agentplexus/web",
    "lint": "npm run lint -w @agentplexus/web",
    "preview": "npm run preview -w @agentplexus/web"
  }
}
